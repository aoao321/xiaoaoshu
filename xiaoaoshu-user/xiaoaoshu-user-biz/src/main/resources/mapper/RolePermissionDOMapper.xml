<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aoao.xiaoaoshu.user.biz.domain.mapper.RolePermissionDOMapper">
    <select id="selectByRoleIds" resultType="com.aoao.xiaoaoshu.user.biz.domain.entity.RolePermissionDO">
        SELECT *
        FROM t_role_permission_rel
        WHERE
        role_id IN
        <foreach collection="roleIds" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
        AND is_deleted=0
    </select>
</mapper>
